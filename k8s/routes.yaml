apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: front-end
    app.kubernetes.io/component: front-end
    app.kubernetes.io/instance: front-end
  name: front-end
spec:
  host: front-end-concession.apps.49.integrate.zone
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: front-end
    weight: 100
  wildcardPolicy: None